spring:
  application:
    #应用名字
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.hexindai.com/credit/config.git
          username: creditlist
          password: creditlist123456
          default-label: master # 配置仓库的分支
          basedir: /Users/yaxingdu/workspace/tiankongdu/spring-cloud-server/config-server/config # 配置本地存储的相对路径
#         search-paths: # 配置仓库的路径


server:
  port: 9090

# 开启健康检查项
management:
  endpoints:
    web:
      exposure:
        include: "*"

# 注册到eureka注册中心
eureka:
  client:
    service-url:
      defaultZone: http://eureka.server1.credit:9091/eureka/,http://eureka.server2.credit:9092/eureka/
